# 版本记录

版本号格式：`大版本.小版本.bug修复`。最新版本在最上方。

每个版本按软件更新日志风格撰写：分「新增 / 修复 / 优化 / 变更」节，面向使用者描述变化（只列有内容的节，空节省略）。

## 1.9.0（2026-08-30）

### 变更

- 主窗口左侧工具列表移除，改为顶部菜单栏「工具」菜单单击打开，屏幕空间全部留给工作区

## 1.8.0（2026-08-28）

### 新增

- Hermes：Headers 页顶部新增自动计算请求头行——Host 由 URL 推导、Content-Length 按请求体估算、Content-Type 按请求体种类推导；只读灰显、不随请求保存，手工填写同名头时对应自动行自动隐藏
- Hermes：默认请求头补充 `Cache-Control: no-cache`，对齐 Postman 现行默认头全集

### 变更

- Hermes：同名请求头以最下方（最后出现）的启用项为准，与 Content-Type 去重逻辑叠加后，同名单值头恒只发送一个值

## 1.7.0（2026-08-28）

### 新增

- Hermes：新建请求预填默认请求头（Accept、Accept-Encoding、User-Agent、Connection），可在 Headers 页编辑或停用
- Hermes：响应体自动解压（gzip / deflate / br，支持多层压缩）并按 charset 解码显示；请求头按用户所写原样发出，不再被自动解压逻辑改动

### 修复

- Hermes：显式设置的 Content-Type 头与请求体所选 Content-Type 重复发出的问题，同名单值头只发送一个值

### 优化

- Hermes：切换集合树中的请求时清空响应区，并自动回填该请求最近一次的历史响应

## 1.6.0（2026-08-27）

### 新增

- 新工具 **Oedipus 解码**：支持 Base64、URL、XML 实体解码，以及 JWT 解码（header/payload 美化输出，签名段原样标注）；非法输入明确报错
- Oedipus：记住上次使用的解码方式，重启后恢复

## 1.5.0（2026-08-27）

### 新增

- 新工具 **Cadmus 编码**：支持 Base64 与 URL 编码，输出一键复制
- Cadmus：记住上次使用的编码方式，重启后恢复

## 1.4.1（2026-08-27）

### 修复

- 系统显示缩放（高 DPI）下界面控件裁剪、错位的问题

### 优化

- Hermes：地址栏随窗口拉伸，发送/保存按钮按内容自适应
- 构建脚本在 cmd 窗口中的中文提示正常显示

## 1.4.0（2026-08-27）

### 新增

- Hermes：界面布局持久化——三个分隔条位置按比例记忆，调整即保存，重启后按比例还原；多标签页共享同一布局

## 1.3.0（2026-08-27）

### 变更（破坏性）

- 插件契约 `ITool` 改为依赖注入模式：插件自行注册内部服务，每个工具拥有独立 IoC 容器；全部第一方插件同步适配（用户授权的一次性契约变更）

### 优化

- 工具服务生命周期规范化：跨标签页共享的服务常驻，视图相关服务随标签页生灭

## 1.2.0（2026-08-27）

### 新增

- 日志级别可配置：程序目录 `daedalus.json` 支持全局默认级别与按插件单独提级；配置缺失或损坏时回退默认，不影响启动

## 1.1.0（2026-08-27）

### 新增

- 主窗口启动时默认最大化

### 修复

- Hermes 执行后事件脚本时偶发"context 已 unload"错误（插件加载机制调整，根除该问题）
- 插件部署时缺少依赖描述文件导致的加载失败

## 1.0.1（2026-08-27）

### 新增

- 一键构建脚本 `build.ps1` / `build.bat`：编译整个解决方案并自动将插件部署到主程序输出目录

## 1.0.0（2026-08-27）

首个全功能版本（重大里程碑）。

### 新增

- 插件化工具箱外壳：工具/格式化器插件自动发现与加载、单个插件失败不影响整体、可关闭多标签页、按天滚动日志、异常兜底提示
- **Proteus 格式化工具**：JSON / XML 格式化、压缩、校验（错误定位行列）、语法高亮
- **Hermes HTTP 客户端**：请求编辑与发送、重定向跳转链逐跳查看、Cookie 共享与请求级覆盖、忽略证书校验开关、集合树管理、环境变量与悬浮编辑、Postman / cURL 导入、后事件脚本、历史记录按天保存与按月归档、历史全文搜索

### 修复

- 发布验证中发现：部署形态下 Hermes 后事件脚本引擎因缺少依赖文件不可用

## 0.11.0（2026-08-27）

### 新增

- Hermes：历史自动归档——30 天前的历史记录按月压缩打包（优先使用 7z 最大压缩，缺失时回退内置 zip），校验通过后才删除原文件
- Hermes：历史搜索——对 URL / 请求头 / 请求体 / 响应等全文关键词搜索；支持"搜索更久"逐包检索归档文件，可随时停止

## 0.10.0（2026-08-27）

### 新增

- Hermes：导入 Postman Collection v2.1 与 Environment v1 导出文件（不支持的版本明确报错）
- Hermes：粘贴 Chrome "Copy as cURL" 命令导入为请求
- Hermes：后事件脚本——响应返回后执行 JavaScript（对齐 Postman `pm` API 子集，沙箱内运行、限制内存与超时），脚本对环境变量的修改立即保存

## 0.9.0（2026-08-27）

### 新增

- Hermes 完整界面，手工可用：集合树管理（增删改、拖拽）、请求编辑（参数/请求头/请求体/选项/脚本五页）、响应区按跳转链逐跳展示、历史列表（查看、重放）、环境管理与切换、`{{变量}}` 悬浮就地编辑、设置面板（修改即保存）
- 关闭未保存的请求编辑时提示确认

## 0.8.0（2026-08-27）

### 新增

- Hermes HTTP 引擎：请求发送与取消、重定向手动跟随（301/302/303/307/308，10 跳上限、循环检测）、Cookie 跨请求共享、"忽略服务器证书校验"开关

## 0.7.0（2026-08-27）

### 新增

- Hermes 数据层：集合、环境、历史、设置均以 JSON 文件本地存储；数据文件损坏时自动备份并以空数据启动

## 0.6.0（2026-08-27）

### 新增

- Proteus 格式化工具界面：JSON / XML 格式化、压缩、校验（错误显示行列）、语法高亮、输出一键复制；记住上次使用的格式与缩进

## 0.5.0（2026-08-27）

### 新增

- 第一方格式化器插件：JSON（美化/压缩，错误定位字符行列）与 XML（美化/压缩，防 XXE）

## 0.4.0（2026-08-26）

### 新增

- 主程序外壳：工具列表 + 可关闭多标签页、插件加载失败在状态栏提示、Serilog 按天滚动日志、未处理异常兜底提示

## 0.3.0（2026-08-26）

### 新增

- 插件契约定稿（工具 `ITool` / 格式化器 `IFormatter` / 宿主服务 `IToolHost`）
- 插件加载器：扫描插件目录、独立加载上下文、单个插件失败隔离

## 0.2.0（2026-08-26）

### 新增

- 工程脚手架：解决方案与全部工程、统一构建配置、中央包管理、插件编译后自动部署到主程序插件目录

## 0.1.0（2026-08-26）

- 设计阶段完成：需求、架构、代码规范、插件设计与实施计划定稿（尚无代码）
